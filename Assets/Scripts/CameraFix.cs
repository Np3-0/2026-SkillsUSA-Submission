using UnityEngine;

public class CameraFix : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //Camera.main.backgroundColor = new Color(114f, 117f, 26f, 0f);
    }
}
